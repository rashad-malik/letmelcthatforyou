"""
Application version — single source of truth.

Update this string when tagging a new release.
The value should match the git tag minus the 'v' prefix
(e.g. tag v2.0.3-beta → version "2.0.3-beta").
"""

__version__ = "2.2.0-beta"
